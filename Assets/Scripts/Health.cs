using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class Health
{
    [SerializeField] int health = 10;
    Item item;

    public Health(Item item, int health )
    {
        this.health = health;
        this.item = item;
    }
    public void TakeDamage(int amount=1)
    {
        if (item.mayTakeDamage)
        {
            health -= amount;

            item.gotHit.Play();

            if (health <= 0)
            {
                item.Die();
            }
        }
    }
}
*/